append cvmdck_mtd, colvar_mtd, parvar_mtd files from all runs  in the right order (remove any redundnt lines)
copy appended files to a folder 'analysis'
correctly difine parameters and the mtd_extract code in the "run_new.sh"
run the command: "sh run_new.sh"
'Pu.dat' (probability) file will be generated 
